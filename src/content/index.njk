---
layout: home.njk
numberOfLatestProjectsToShow: 4
---

  <div class="rounded-lg font-Test2 pointer-events-none relative flex overflow-hidden shadow-lg shadow-indigo-600 border border-dashed border-y-indigo-600 py-2 hover:text-white hover:bg-indigo-600">
    <div class="animate-marquee text-indigo-600 flex min-w-full shrink-0 items-center justify-around  ">
          <span class="mx-4 text-2xl">TRANSNATIONAL ANTI-COLONIAL TRANS★FEMINIST COUNTER CLOUD ACTION DAY</span>
    </div>
    <div aria-hidden="true" class="text-indigo-600 animate-marquee flex min-w-full shrink-0 items-center justify-around ">
             <span class="mx-4 text-2xl">TRANSNATIONAL ANTI-COLONIAL TRANS★FEMINIST COUNTER CLOUD ACTION DAY</span>
    </div>
  </div>
<div class="rounded-lg place-items-center grid grid-cols-1 md:grid-cols-2 lg:gap-8 shadow-lg shadow-indigo-600">
  <div class="p-8 ">
    <h1>Research Workshop 2024: Content/Form</h1>
    <p class="py-8">The participants of this year’s workshop will present their reflections on ways to collapse traditional workflows of academic publishing, drawing form and content more closely together. </p>
    <button class="btn btn-outline hover:text-white hover:bg-indigo-600 border-indigo-600 text-indigo-600 font-Test2">More info</button>

  </div>
  <div class="">
    <img src="https://data.transmediale.de/assets/_1600xAUTO_crop_center-center_none/tm24_ResearchWorkshop2024_HKW_AngieStardustFoyer_2403_CCBYNCSA_LauraFiorio_002.jpg" class="rounded-lg"/>
  </div>
</div>

{# <p>Any custom bits only for the home page can be added... </p> #}

<div>

{% set projectsCount = collections.projects | length %}
{% set latestProjectsCount = projectsCount | min(numberOfLatestProjectsToShow) %}
{# <h1>Recent Projects</h1> #}

{% set projectlist = collections.projects | head(-1 * numberOfLatestProjectsToShow) %}
{% set projectlistCounter = projectsCount %}
{% include "projectlist.njk" %}

{% set moreProjects = projectsCount - numberOfLatestProjectsToShow %}
{% if moreProjects > 0 %}
<h1 class='font-Test2 mt-30 text-xl flex items-center justify-center text-indigo-600 hover:text-white hover:bg-indigo-600 border border-dashed border-indigo-600'> <a href="/projects/">+MORE PROJECTS+ </a></p>
{# {{ moreProjects }} more project{% if moreProjects != 1 %}s{% endif %}can be seen #}
{% endif %}

{# List every content page in the project #}
{#
<ul>
	{%- for entry in collections.all %}
	<li><a href="{{ entry.url }}"><code>{{ entry.url }}</code></a></li>
	{%- endfor %}
</ul>
#}

</div>



<div class="text-center px-8 py-4 mt-4 border-indigo-600 border border-dotted shadow-lg shadow-indigo-600">
        
    <h3 class="mb-4 rounded-md">Project Categories</h3>
    {% for tag in collections.all | getAllTags | filterTagList %}
        {% set tagUrl %}/tags/{{ tag | slugify }}/{% endset %}
        <a href="{{ tagUrl }}" class="font-Test3 font-bold project-tag rounded-md hover:text-white hover:bg-indigo-600 border-indigo-600 border text-indigo-600 border-dotted badge badge-outline badge-lg">{{ tag }}</a>{%- if not loop.last %} {% endif %}
    {% endfor %}
</div>
