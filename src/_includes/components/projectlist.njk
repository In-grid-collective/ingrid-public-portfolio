{# {%- css %}.projectlist { counter-reset: start-from {{ (projectlistCounter or projectlist.length) + 1 }} }{% endcss %}

<div class="projects">
	 <div class="row row-cols-1 row-cols-sm-2 row-cols-md-4">
		{% for project in projectlist | reverse %}
			<div class="col projectlist-item{% if project.url == url %} projectlist-item-active{% endif %}">
				<a href="{{ project.url }}" class="projectlist-link">

					{% if project.data.icon %}
					{% set imgurl = "."+project.url + project.data.icon %}
					{% image imgurl, "A possum parent and two possum kids hanging from the iconic red balloon" %}
					{% endif %}

					<h3>{% if project.data.title %}{{ project.data.title }}{% endif %}</h3>
				</a>

				{# Comment the line below out if you don't want the date showing #}
				{# 
				<time class="projectlist-date" datetime="{{ project.date | htmlDateString }}">{{ project.date | readableDate("LLLL yyyy") }}</time>
			</div>
		{% endfor %}
	</div>
</div> #}


{# Old bootstrap above for reference #}


{%- css %}.projectlist { counter-reset: start-from {{ (projectlistCounter or projectlist.length) + 1 }} }{% endcss %}
<div class="sm:p-8">

	{#<details>
		<summary class="normal-case font-Title text-3xl text-primary ">
			View
		</summary>
		<ul class="p-2 bg-base-100 font-Title">
			<li><a class="font-Title text-3xl text-primary border-primary  ">Grid view</a></li>
			<li><a class="font-Title text-3xl text-primary border-primary ">List view</a></li>
		</ul>
	</details>#}
    <div class="columns-1 gap-2 sm:columns-2 sm:gap-8 md:columns-3 m-2 lg:columns-4 min-h-full [&>img:not(:first-child)]:mt-8 ">
		{% for project in projectlist | reverse %}
			<div class="rounded-md break-inside-avoid-column hover:text-black hover:bg-zinc-300 border border-dashed border-indigo-600 shadow-lg shadow-indigo-600 hover:border-white {% if project.url == url %} projectlist-item-active{% endif %}">

				<a href="{{ project.url }}" class="projectlist-link p-8 inline-block ">

					{% if project.data.icon %}
					{% set imgurl = "."+project.url + project.data.icon %}
					{% image imgurl, "A possum parent and two possum kids hanging from the iconic red balloon" %}
					{% endif %}

					<h3>{% if project.data.title %}{{ project.data.title }}{% endif %}</h3>
					<time class="projectlist-date font-main " datetime="{{ project.date | htmlDateString }}">{{ project.date | readableDate("LLLL yyyy") }}</time>

				</a>		
			</div>
		{% endfor %}
	</div>
</div>